# 130. 被围绕的区域
## 一、 题目描述

给你一个 `m*n` 的矩阵 `board` ，由若干字符 `X` 和 `O` 填充，找出所有被 `X` 围绕的 `O` 区域，将其替换为 `X` 。

```
输入：board = [["X","X","X","X"],["X","O","O","X"],["X","X","O","X"],["X","O","X","X"]]
输出：[["X","X","X","X"],["X","X","X","X"],["X","X","X","X"],["X","O","X","X"]]
解释：被围绕的区间不会存在于边界上，换句话说，任何边界上的 'O' 都不会被填充为 'X'。 任何不在边界上，或不与边界上的 'O' 相连的 'O' 最终都会被填充为 'X'。如果两个元素在水平或垂直方向相邻，则称它们是“相连”的。
```
$$
\begin{matrix}
    X & X & X & X\\
    X & \color{red}{O} & \color{red}{O} & X\\
    X & X & \color{red}{O} & X\\
    X & O & X & X
\end{matrix}
\qquad
\Rightarrow
\qquad
\begin{matrix}
    X & X & X & X\\
    X & \color{red}{X} & \color{red}{X} & X\\
    X & X & \color{red}{X} & X\\
    X & O & X & X
\end{matrix}
$$

## 二、 思路分析
要找出里面的区域，可以遍历里面的元素，然后找到处于中间的 `O` ，通过搜索得到整个相连的 `O` ，但是此时还需要判断这块连通的 `O` 集合是否处于边界上，比较麻烦。此时可以换一种思路进行分析，如果我们能将边界上的所有连通的 `O` 找出来，然后将其替换成另一种元素，再遍历 `board` 时遇到的 `O` 即为被 `X` 包围的了。

### 1. 深度优先搜索
```cpp
class Solution {
private:
    void dfs(int i, int j, vector<vector<char>>& b) {
        int row = b.size(), col = b[0].size();
        if (i < 0 || i >= row || j < 0 || j >= col || b[i][j] != 'O') {
            return;
        }
        b[i][j] = '-';
        dfs(i - 1, j, b);
        dfs(i, j - 1, b);
        dfs(i + 1, j, b);
        dfs(i, j + 1, b);
    }
public:
    void solve(vector<vector<char>>& board) {
        int row = board.size();
        if (!row) {
            return;
        }
        int col = board[0].size();
        if (!col) {
            return;
        }

        for (int i = 0; i < row; ++i) {
            if (board[i][0] == 'O') {
                dfs(i, 0, board);
            }
            if (board[i][col - 1] == 'O') {
                dfs(i, col - 1, board);
            }
        }

        for (int j = 1; j < col - 1; ++j) {
            if (board[0][j] == 'O') {
                dfs(0, j, board);
            }
            if (board[row - 1][j] == 'O') {
                dfs(row - 1, j, board);
            }
        }

        for (int i = 0; i < row; ++i) {
            for (int j = 0; j < col; ++j) {
                if (board[i][j] == 'O') {
                    board[i][j] = 'X';
                }
                if (board[i][j] == '-') {
                    board[i][j] = 'O';
                }
            }
        }

    }
};
```
时间复杂度： $O(mn)$ ；
空间复杂度： $O(mn)$ 。

### 2. 广度优先搜索
```cpp
class Solution {
private:
    int DIR[4][2] = {{1, 0}, {-1, 0}, {0, 1}, {0, -1}};
    /** 写成递归形式会TLE
    void bfs(int i, int j, vector<vector<char>>& board) {
        int row = board.size(), col = board[0].size();
        queue<int> q;
        q.push(i * col + j);
        while (!q.empty()) {
            int idx = q.front();
            q.pop();
            int rowIdx = idx / col;
            int colIdx = idx % col;
            board[rowIdx][colIdx] = '-';
            for (int i = 0; i < 4; ++i) {
                int currRowIdx = rowIdx + DIR[i][0];
                int currColIdx = colIdx + DIR[i][1];
                //if (currRowIdx >= 0 && currRowIdx < row && currColIdx >= 0 && currColIdx < col && board[currRowIdx][currColIdx] == 'O') {
                //    q.push(currRowIdx * col + currColIdx);
                    //board[currRowIdx][currColIdx] = '-';
                //}
                if (currRowIdx < 0 || currRowIdx >= row || currColIdx < 0 || currColIdx >= col || board[currRowIdx][currColIdx] != 'O') {
                    continue;
                }
                q.push(currRowIdx * col + currColIdx);
            }
        }
    }
    **/
public:
    void solve(vector<vector<char>>& board) {
        int row = board.size();
        if (!row) {
            return;
        }
        int col = board[0].size();
        if (!col) {
            return;
        }

        queue<int> q;
        for (int i = 0; i < row; ++i) {
            if (board[i][0] == 'O') {
                //bfs(i, 0, board);
                q.push(i * col);
            }
            if (board[i][col - 1] == 'O') {
                //bfs(i, col - 1, board);
                q.push(i * col + col - 1);
            }
        }

        for (int j = 1; j < col - 1; ++j) {
            if (board[0][j] == 'O') {
                //bfs(0, j, board);
                q.push(j);
            }
            if (board[row - 1][j] == 'O') {
                //bfs(row - 1, j, board);
                q.push((row - 1) * col + j);
            }
        }

        while (!q.empty()) {
            int idx = q.front();
            q.pop();
            int rowIdx = idx / col;
            int colIdx = idx % col;
            board[rowIdx][colIdx] = '-';
            for (int i = 0; i < 4; ++i) {
                int currRowIdx = rowIdx + DIR[i][0];
                int currColIdx = colIdx + DIR[i][1];
                //if (currRowIdx >= 0 && currRowIdx < row && currColIdx >= 0 && currColIdx < col && board[currRowIdx][currColIdx] == 'O') {
                //    q.push(currRowIdx * col + currColIdx);
                    //board[currRowIdx][currColIdx] = '-';
                //}
                if (currRowIdx < 0 || currRowIdx >= row || currColIdx < 0 || currColIdx >= col || board[currRowIdx][currColIdx] != 'O') {
                    continue;
                }
                q.push(currRowIdx * col + currColIdx);
            }
        }

        for (int i = 0; i < row; ++i) {
            for (int j = 0; j < col; ++j) {
                if (board[i][j] == 'O') {
                    board[i][j] = 'X';
                }
                if (board[i][j] == '-') {
                    board[i][j] = 'O';
                }
            }
        }

    }
};
```
时间复杂度： $O(mn)$ ；
空间复杂度： $O(mn)$ 。

还有一种方法是并查集，思路与深搜广搜完全一样。有兴趣可以自己实现一下。